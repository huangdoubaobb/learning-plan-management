import {
  s
} from "./chunk-P7LO4TJ2.js";
import {
  ce,
  ne
} from "./chunk-YZMXKYHC.js";
import {
  computed,
  createElementBlock,
  nextTick,
  normalizeStyle,
  onMounted,
  openBlock,
  ref,
  renderSlot,
  toRef,
  watch
} from "./chunk-X3QCHAKX.js";
import "./chunk-DC5AMYBS.js";

// node_modules/vue-data-ui/dist/SparkTooltip-BiseJed3.js
var R = {
  __name: "SparkTooltip",
  props: {
    svgRef: { type: Object },
    x: { type: Number, required: true },
    y: { type: Number, required: true },
    prevX: { type: Number, required: true },
    prevY: { type: Number, required: true },
    offsetY: { type: Number, default: 0 },
    background: { type: String },
    backgroundOpacity: { type: Number, default: 100 },
    borderRadius: { type: Number, default: 2 },
    borderWidth: { type: Number, default: 0 },
    borderColor: { type: String, default: "#FFFFFF" },
    color: { type: String },
    fontSize: { type: Number }
  },
  setup(d) {
    const e = d, a = toRef(e.svgRef), o = ref(null), n = ref(0), l = ref(0), p = ref(false), f = computed(() => ne(e.background, e.backgroundOpacity)), s2 = async () => {
      if (!a.value || !o.value) return;
      const t = ce({
        svgElement: a.value,
        element: o.value,
        x: e.x,
        y: e.y,
        offsetY: e.offsetY
      }), r = ce({
        svgElement: a.value,
        element: o.value,
        x: e.prevX,
        y: e.prevY,
        offsetY: e.offsetY
      });
      !t || !r || (p.value = false, n.value = r.top, l.value = r.left, await nextTick(), setTimeout(() => {
        p.value = true, n.value = t.top, l.value = t.left;
      }, 50));
    };
    return onMounted(s2), watch(
      () => [e.x, e.y, e.prevX, e.prevY],
      () => s2(),
      { immediate: true }
    ), (t, r) => (openBlock(), createElementBlock("div", {
      ref_key: "tooltipRef",
      ref: o,
      class: "vue-data-ui-spark-tooltip",
      style: normalizeStyle({
        position: "fixed",
        top: `${n.value}px`,
        left: `${l.value}px`,
        pointerEvents: "none",
        background: f.value,
        color: e.color,
        fontSize: `${e.fontSize}px`,
        borderRadius: `${e.borderRadius}px`,
        border: `${e.borderWidth}px solid ${e.borderColor}`,
        transition: p.value ? "top 0.3s ease-out, left 0.3s ease-out" : "none"
      })
    }, [
      renderSlot(t.$slots, "default", {}, void 0, true)
    ], 4));
  }
};
var F = s(R, [["__scopeId", "data-v-c8e27542"]]);
export {
  F as default
};
//# sourceMappingURL=SparkTooltip-BiseJed3-SPJ5MSUC.js.map
