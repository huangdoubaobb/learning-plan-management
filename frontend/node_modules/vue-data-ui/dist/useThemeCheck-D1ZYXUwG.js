import { computed as a } from "vue";
function i() {
  const n = a(() => (e) => !e || !e.theme ? !1 : [
    "default",
    "dark",
    "celebration",
    "celebrationNight",
    "hack",
    "zen",
    "concrete",
    "minimal",
    "minimalDark"
  ].includes(e.theme));
  function t(e) {
    e?.debug && console.warn(`Vue Data UI - "${e?.theme}" is not a valid theme.

Available themes:

- default (or "")
- dark
- celebration
- celebrationNight 
- hack
- zen
- concrete`);
  }
  return {
    isThemeValid: n,
    warnInvalidTheme: t
  };
}
export {
  i as u
};
